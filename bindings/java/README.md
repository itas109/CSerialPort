# CSerialPort for Java

```
swig: 4.1.0
cmake: 3.8.2
java: 1.8.0
```

## Build

```
mkdir bin
cd bin
cmake .. 
cmake --build .
```

## Run

```
cd bin
java -Djava.library.path=. -jar CommJava.jar
```

